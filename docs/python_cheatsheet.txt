if somevalue == 17:
  do_stuff()
else:
  bla()

try:
  do_stuff()
except ValueError:
  oh_shit()



if len(some_input_string) == 0:

#single line comment


"///foo".lstrip('/') -> "foo"
"123".isdigit()  -> True

elif not(some_boolean):

new_instance = SomeClass(someinitparam=somevalue)

for value in list:
  do_stuff()


if elem in list:

if somekey in somedict:


select random element from list:
from random import choice
print choice(foo)

json.dumps does not handle datetime, need to supply custom converter

newlist = list()
filledlist = [1,"bla"]

parse int from string>
try:
  return int(s)
except exceptions.ValueError:
  return float(s)